# 日志配置文件
# 文件最大大小 (字节)
file_max_size=10485760

# 日志路径
log_path=logs

# 日志文件前缀名
prefix_file_name=comprehensive_t

# 日志级别: 1=FATAL, 2=WARNING, 4=NOTICE, 8=TRACE, 16=DEBUG
type=31

# 日志模式: 1=本地同步, 2=线程异步
model=2

# 配置转储路径
dumppath=log_conf_dump 